pipeline {
 agent any
    stages {
        stage('test') {
            steps {
                sh 'echo Ejecucion de comandos'
                sh 'pwd'
                sh 'whoami'
                sh 'ls -la'
            }
        }
    }
}
